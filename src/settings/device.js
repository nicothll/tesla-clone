const SIZE = {
    mobile: '600px',
    tablet: '1024px',
    laptop: '2560px',
  }
  
export const device = {
  mobile: `(max-width: ${SIZE.mobile})`,
  tablet: `(max-width: ${SIZE.tablet})`,
  laptop: `(max-width: ${SIZE.laptop})`,
}